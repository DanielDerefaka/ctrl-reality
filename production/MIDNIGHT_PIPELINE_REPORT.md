# Midnight Express art-pipeline audit and scaffold

Date: 2026-09-23. **Live NYRA_MASK proof is blocked; no generation success claimed.**

## Requested end report

1. **Branch / commit:** `feature/midnight-mask-pipeline`, branched from the clean preserved Zero Hour commit `600260458fee94ed03d11b11a64bdafcfcf446ff`. This report accompanies `chore: scaffold Midnight Express mask generation pipeline`; the final task reply reports the new SHA. No history rewritten. Existing floor tag `floor-prototype-v1` stays at `88be3bb47c45ac16f5c4d0acb5d61affec870a5f`.
2. **Existing prototype checks:** Before pipeline implementation, 35 unit tests passed / 0 failed; static check passed (24 modules / 25 local references); fresh actual-input browser suite 34 passed / 0 failed, with zero page/console errors, missing/failed requests or outside-game requests. Saved in `production/midnight-pipeline/`; fresh screenshots in its `prototype-browser/screenshots/`. Historical screenshots/docs untouched. The browser harness gained only an optional output directory so it would not overwrite prior receipts. Final full unit suite: 55 passed / 0 failed (20 new offline pipeline tests); static check unchanged and passed. No physical-device or real-provider generation test.
3. **Configured MCP names:** `slither-analyzer`, `solana-fender`, `unified-vuln-db`, `farofino`, `eden`, `inspo`, `node_repl`, `computer-use` in user config. Project `.codex/config.toml` absent. `node_repl.env` is a nested table, not a separate server. Effective CLI list additionally includes `codex_app` and `cua_repl`: CLI enabled entries are cua_repl, eden, farofino, inspo, node_repl, slither-analyzer, solana-fender, unified-vuln-db; codex_app and computer-use are disabled in that CLI view. All CLI auth-status values are `unsupported`; this is not a connection test. Session tool availability differs from the separate CLI configuration: inspo and node_repl tools are exposed; direct cua_repl tools and app tools are also present. Sanitized full names in `midnight-pipeline/session-tools.json`. The other configured services have no callable tools exposed here; no handshake or authentication success claimed. `/mcp` was not run because this task is not an interactive Codex TUI; do not treat a shell `/mcp` as an equivalent command.
4. **Genuine 404 MCP:** None configured or exposed. No 404 URL, package or tool invented. The recipe is not an MCP connection.
5. **Official recipe commit:** `/Users/dx/Documents/404-game-recipe` at `4effad311c5e137bca316257259fe5bffd6737de`. Read 404.md, GAME.md, asset-contract, concept-images, traps, official wrapper and verifier instructions/source. Its existing package-lock modification was observed and left alone.
6. **Official self-test:** `npm run selftest` exit 0. Three intentionally bad fixtures flagged; 4/4 clean outside-tree fixtures passed; expected instancing, mounting and declared-size checks passed; loader 7/7 checks passed. Exact output: `midnight-pipeline/official-selftest.txt`. Its deliberately bad fixture warnings do not mean the self-test failed. Its generated fixture sheets are not NYRA_MASK sheets.
7. **Higgsfield credentials:** Both fields absent initially; `.env.local` created with blank HF_API_KEY_ID/HF_API_KEY_SECRET, mode 0600 and ignored. No auth request or paid generation attempted. Account-specific image model, documentation URL, endpoint and body still needed. Public shared docs alone do not prove account access.
8. **RunPod credentials/endpoint:** RUNPOD_404_BASE_URL and VLLM_API_KEY absent initially and blank in the scaffold. No endpoint/model/auth check possible, no pod launched and no GPU purchased. Tool checks `/v1/models` when configured; default model is the pinned recipe's Tooony133/Qwen-3.6-27B-AronHorn.
9. **Tools created:** `tools/higgsfield-reference.mjs`, `tools/generate-404-asset.mjs`, shared `tools/art-pipeline/common.mjs`, and `tests/art-pipeline.test.mjs`. Created `.env.example`, ignored blank `.env.local`, mask prompt/model-config example, pipeline guide, resume prompt, scoped authority notices and audit logs. `.gitignore` already had the exact requested env exclusions; verified rather than rewritten. Main game untouched.
10. **Commands:** `pwd`; `git status --short --branch`; `git rev-parse HEAD`; `codex mcp list --json` with allowlisted name/enabled/auth fields only; section-name-only TOML inspection; recipe reads and commit lookup; `npm run selftest` in recipe; `npm test`; `npm run check`; `SMOKE_OUT=../production/midnight-pipeline/prototype-browser/ npm run smoke`; `node --test tests/art-pipeline.test.mjs`; both generation CLI preflights; `git diff --check`; `git check-ignore .env.local`; focused Git commit. Exact CLI preflight commands below. No secrets or full config printed.
11. **Nyra reference path:** None — no image generated. Only the brief exists at `production/asset-receipts/NYRA_MASK/reference-prompt.txt`. Future output paths are documented separately and must not be presented as existing images.
12. **Three candidate paths:** None — no model request made. No hand-written substitute mask or fake response created.
13. **Verification sheet:** None for NYRA_MASK. Official fixture self-test success is evidence about the verifier only.
14. **Warnings/failures:** Higgsfield preflight exit 1: `MISSING_CREDENTIALS: Missing local variables: HF_API_KEY_ID, HF_API_KEY_SECRET`. RunPod preflight exit 1: `MISSING_CREDENTIALS: Missing local variables: RUNPOD_404_BASE_URL, VLLM_API_KEY`. Both stop before any network request. Model config example deliberately has reviewed=false and no endpoint; filling only credentials is insufficient. Account-specific API integration, actual model output, named groups/pivots, 0.24m measurements, warnings and visual quality remain unproved. Early contract lint is not a sandbox. Official verifier uses CDN Three.js 0.169.0; gameplay-camera compatibility with preserved runtime 0.180.0 awaits approved integration. Documentation .md fetches were inaccessible through the browser tool; corresponding HTML pages were read successfully. Initial mistaken tools/wrap.mjs lookup was corrected to the actual harness/wrap.mjs. An evidence-write approval review timed out; its smaller retry succeeded, leaving no permission blocker.
15. **Git status:** All intended source/docs/evidence committed; final task reply confirms status. `.env.local` remains ignored and untracked. No game files, floor captures, Zero Hour receipts, vendor files or credentials staged.
16. **User action:** Populate the four variables locally, supply/select the authenticated Higgsfield image model's current documentation and request schema, and provide the ready RunPod-hosted model endpoint. Do not paste secrets into chat. Then resume prompt 10. After real reference + three candidates + verifier sheet exist, inspect them and approve a candidate before any game integration. No candidate approval requested prematurely.

## Preflight commands and exact results

```sh
node tools/higgsfield-reference.mjs --config production/asset-receipts/NYRA_MASK/higgsfield-model.example.json
# exit 1: missing HF_API_KEY_ID, HF_API_KEY_SECRET
node tools/generate-404-asset.mjs --reference production/asset-receipts/NYRA_MASK/reference/NYRA_MASK-reference.png
# exit 1: missing RUNPOD_404_BASE_URL, VLLM_API_KEY
```

The reference argument above deliberately names the future location; credentials are checked first, so no image was read and no model contacted. No completed-generation receipt was written. Offline tests use explicit protocol fixtures in temporary directories, delete them afterward, and are not generation evidence.

Full operational instructions and source links: `docs/MIDNIGHT_EXPRESS_PIPELINE.md`. Next prompt: `prompts/10_MIDNIGHT_MASK_PIPELINE_PROOF.md`. The pipeline is ready for account configuration and live validation, not yet proven end to end.
