# Discard log

2026-09-24: User explicitly replaced previous game direction with CTRL//REALITY and requested deleting its files. Removed SECOND HANDS, Zero Hour, Midnight Express runtime, old tests, old screens and obsolete pack documentation from the working tree. Previous history remains at `archive-before-ctrl-reality`; no history was rewritten. Retained only reusable dependency/tool infrastructure and local secret configuration. New briefs and Mara concept were copied into the new production tree.

The old top-down circle, drone shooting and echo/train mechanics are not part of the active build. PAUSE and UNDO were intentionally left locked rather than represented as completed sectors.

## Visual lock iterations

Three candidate modules per family and explicit SELECTION.md decisions are retained under asset-receipts/. Rejected card backs and bridge side warnings were not relabeled as passes. Earlier candidate sheets are distinct from final exact-runtime verification. Gameplay review rejected broad arch bands, buried luminous inlays, portrait overlap and the unused second image plane. Early screenshot attempts that missed timing are in visual-lock/rejected-captures/. A paused-debugger screenshot attempt timed out; final timed evidence uses normal running-browser screencast frames.
