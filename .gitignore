node_modules/
.DS_Store
*.log
coverage/
.tmp/
production/gate-runs/*.json
production/gate-runs/*.png

.env
.env.*
!.env.example
.codex/config.toml

# Raw direct-capture frames remain local; the 15-second film and receipt are versioned.
production/recovery/recording/frames/
