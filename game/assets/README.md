# Assets

`prototype-assets.js` is temporary floor geometry and is **not submission compliant as final production art**.

For release:

- generate each object through the current 404 recipe,
- create three independent candidates,
- verify from multiple angles,
- preserve receipts,
- select one module,
- update `manifest.js`,
- remove prototype imports from the active build.
